import './style.css';
export const metadata={title:'Agentic Voice',description:'A private, bounded voice and text assistant.'};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
